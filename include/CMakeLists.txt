cmake_minimum_required(VERSION 3.2)

install(FILES dplane.h DESTINATION include)
install(FILES messages.h DESTINATION include)
install(FILES mk5b.h DESTINATION include)
install(FILES vdif.h DESTINATION include)
install(FILES version.h DESTINATION include)